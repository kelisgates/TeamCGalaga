<ContentDialog
    x:Class="Galaga.View.HighScoreDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Galaga.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:Galaga.ViewModel"
    mc:Ignorable="d"
    Title="High Scores"
    PrimaryButtonText="Go Back"
    >

    <ContentDialog.Resources>
        <CollectionViewSource x:Key="SortedHighScores" Source="{Binding HighScores}" IsSourceGrouped="false" />
    </ContentDialog.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Score/Name/Level" Click="SortByScoreNameLevel_Click" Margin="5"/>
            <Button Content="Name/Score/Level" Click="SortByNameScoreLevel_Click" Margin="5"/>
            <Button Content="Level/Score/Name" Click="SortByLevelScoreName_Click" Margin="5"/>
        </StackPanel>
        <ListView Grid.Row="1" ItemsSource="{Binding Source={StaticResource SortedHighScores}}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding PlayerName}" Width="200"/>
                        <TextBlock Text="{Binding Score}" Width="100"/>
                        <TextBlock Text="{Binding Level}" Width="100"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
